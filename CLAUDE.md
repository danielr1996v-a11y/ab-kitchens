@AGENTS.md

# א. בית המטבחים — הקשר לפרויקט

אתר Next.js בעברית (RTL) עבור **א. בית המטבחים** — יצרן מטבחים בירושלים.
הלקוח: **אברהם ביטון**. המפתח: **דניאל רזניקוב, Studio Reznik**.

**דניאל מעדיף עברית, תשובות קצרות, וביצוע עצמאי בלי הרבה שאלות הבהרה.**

---

## פרטי העסק

| | |
|---|---|
| דומיין | ab-kitchens.co.il |
| כתובת | בית ישראל 2, ירושלים |
| טלפונים | 055-2775488 · 055-7799043 |
| אימייל | bk0552775488@gmail.com |
| וואטסאפ | 972552775488 |
| שעות | ראשון-חמישי 10:00-18:00 |
| דירוג Google | 5.0 (11 ביקורות) |
| Facebook Pixel | 981884166678578 |

### ⚠️ אזהרה קריטית
**ABB Kitchens (חדרה) היא חברה אחרת לגמרי.** בגוגל דרייב של הלקוח יש תיקייה
`01 טקסטים לפי עמודים` שמכילה תוכן של ABB — **אסור להשתמש בה.**

---

## סטאק

Next.js 16 · App Router · TypeScript · Turbopack · CSS מותאם (**לא Tailwind**)

- **פונט:** Polin (3 משקלים: 300/400/700) ב-`public/fonts/`
- **פריסה:** Vercel, פרויקט `studio-reznik kitchen`
- **ריפו:** `danielr1996v-a11y/ab-kitchens`
- **ענף תצוגה ללקוח:** `client-preview` — **לעולם לא לדחוף ל-`main` בלי אישור**

---

## חוקי עבודה (מחייבים)

1. **בלי CMS** אלא אם התבקש במפורש
2. **משימה אחת בכל פעם.** לסיים, להציג, לחכות לאישור
3. **בלי px** מלבד קווי שיער. אופקי ב-vw, גבהים ב-vh, גובה שורה ב-em
4. **5vw ריפוד צדדי** דרך `var(--side-padding)`
5. **מחלקות BEM** ב-`app/globals.css`, לא CSS-in-JS
6. **בלי קוד מת.** משהו יוצא משימוש — נמחק מיד
7. **הפיגמה היא סמכות** — אבל הוראה מפורשת של דניאל גוברת עליה
8. **תמיד להריץ `npm run build`** לפני שמדווחים שסיימנו

### תבנית העבודה של דניאל
הוא משתמש בכלי `DevInspector` (כפתור "בחר אלמנט" בפינה) ששולח בקשות תיקון
בפורמט קבוע: סקשן, סלקטור, מצב נוכחי, ומה לשנות. הכלי חסום ב-production.

---

## פיגמה

- **fileKey:** `KG2iA3wDEIlaDQxNRuX8HZ`
- סקשן אודות: `2002:3521` · באנר מובילים: `2016:144` · פוטר: `2022:1255`
- **הפיגמה משתמשת בפונט Ploni — אנחנו תמיד ב-Polin.** לא להעתיק את הפונט משם.

---

## טוקנים מרכזיים

```css
--color-bg: #ffffff;
--color-fg: #1a1a1a;
--color-muted: #767676;
--color-header-bg: #242323;   /* פחם */
--color-surface: #f0f0f0;     /* רקע אודות ופוטר */
--color-border: #e7e7e7;

--color-gold: #ddb143;        /* זהב ממשק - כפתורי פעולה */
--color-logo-gold: #aa8347;   /* זהב הלוגו - נמדד מהקובץ. מספרים, מרקר */

--side-padding: 5vw;
--space-1..7: 8 / 16 / 24 / 32 / 40 / 64 / 88 px
--ease-soft: cubic-bezier(0.32, 0, 0.16, 1);
```

**שני הזהבים שונים ולא להחליף ביניהם.** `--color-gold` לפעולות,
`--color-logo-gold` לזהות המותג.

---

## מבנה דף הבית (סדר נוכחי)

```
Hero → About → StyleGrid → Values → TrustedBy → LeadBanner → Testimonials → Footer
```

---

## דפוסים לשימוש חוזר

| רכיב / מחלקה | מה זה עושה |
|---|---|
| `Reveal.tsx` | עוטף שמוסיף `is-visible` בכניסה למסך. מכבד reduced-motion |
| `Icon.tsx` | ספריית אייקונים אחת, כולם stroke 1.5 על גריד 24 |
| `renderRichText()` | `**מודגש**` |
| `renderHighlight()` | `==מרקר==` בזהב הלוגו |
| `.reveal-mask` | כותרת עולה מתוך מסכה |
| מרקיזה | שתי קבוצות זהות, `min-width: 100%`, translateX. בלי JS |

---

## החלטות שהתקבלו

- **נתיבים בעברית נשמרו** לשימור SEO. תיקיות באנגלית + rewrites ב-`next.config.ts`
- **סליידרים נגזרים מ-`slides.length`** — הוספה/הסרה מסתדרת לבד
- **לוגואים על קנבס אחיד 360×128** — כך היישור מובטח ללא תלות ביחס המקורי
- **noindex בכל דיפלוי שאינו production** דרך `VERCEL_ENV`, בשתי שכבות
- **סקשן האודות לא כולל וידאו** — הפיגמה לא כללה אותו. `VideoPlayer.tsx` נמחק

---

## 🔴 פתוח — דורש את הלקוח

1. **הטופס בפוטר לא מחובר לשום יעד.** `action="#"`. פנייה תיעלם
2. **~~שמות ההמלצות~~** — ✅ נסגר. 11 המלצות אמיתיות עם שמות שוחזרו
   מארכיון האתר הישן ל-`docs/old-site-content.md`. שבע באתר.
   נותר לוודא מול פרופיל הגוגל שכולן באמת 5 כוכבים
3. **~~סתירת ותק~~** — ✅ נסגר. האתר הישן אומר במפורש "למעלה מ-30 שנה".
   הבחירה ב-30 מאוששת
4. **אין אף תמונה אמיתית** — הכל סטוק. זה סותר את הטענה "אנחנו המפעל".
   נדרשים צילומים של אברהם, הנגרייה, אולם התצוגה
5. **עמודים משפטיים** נכתבו על ידי Claude — **חייבים בדיקת עורך דין**,
   במיוחד הצהרת הנגישות
6. **פרופיל Google Business כפול** — מפצל SEO מקומי
7. **נתוני SEO למיגרציה:** Search Console, Analytics, sitemap מ-Duda

---

## עמוד אודות — תוכנן, טרם נבנה

מבנה מאושר, **בלי תמונות** (אין חומר גלם). הטיפוגרפיה נושאת את העמוד.

1. כותרת — "30 שנה של מטבחים"
2. הסיפור — שתי פסקאות, מרקר על "היא המפעל עצמו"
3. מספרים — `30` שנות ניסיון · `2` דורות · `מאות` מטבחים. בזהב הלוגו, בלי אייקונים
4. **התהליך — 4 שלבים.** פריסה מאושרת: רשימה מימין, לוח מתחלף משמאל בריחוף
5. למה אנחנו — ארבעת הבידולים, מעמיק ולא משכפל את דף הבית
6. CTA — הפאנל הכהה של LeadBanner בלי תמונה

### ארבעת השלבים
```
01 פגישה ותכנון        — פגישה, מידות, תוכנית והדמיה, בחירת גימורים
02 הצעת מחיר ואישור    — מחיר סופי, בלי הפתעות, לוח זמנים ברור
03 ייצור במפעל שלנו    — נגרות ושיש אצלנו, לא קבלני משנה  ← הטענה המרכזית
04 התקנה ואחריות       — התקנה, כוונון, ואחריות שממשיכה
```

**במובייל אין ריחוף** — הפירוט צריך להיות פתוח תמיד.
