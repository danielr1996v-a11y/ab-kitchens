# א. בית המטבחים — PROJECT.md

> מקור האמת היחיד לפרויקט הזה.
> חוקים גלובליים: `~/.claude/CLAUDE.md` · חוקי עיצוב: `~/.claude/design/RULES.md`

עודכן: 26.08.2026 · סטטוס: **באוויר בתצוגת לקוח, עמוד אודות טרם נבנה**

## 1. Brief

| | |
|---|---|
| **מטרה אחת** | שבעל בית שמתכנן מטבח יפנה לאברהם — ויבין שהוא מדבר עם **המפעל**, לא עם משווק |
| **בעיה** | האתר הישן (Duda) לא מעביר את הטענה המרכזית: הנגרות והשיש נעשים אצלם, לא אצל קבלני משנה |
| **קהל יעד** | בעלי בתים בירושלים והסביבה שמתכננים מטבח |
| **ההבטחה** | "אנחנו המפעל" — ייצור עצמי, שני דורות, 30 שנה |
| **מיצוב** | יצרן, לא מתווך |
| **Tone of Voice** | סמכותי · חם · בלי הבטחות מנופחות |

**פרטי העסק**

| | |
|---|---|
| לקוח | **אברהם ביטון** |
| דומיין | ab-kitchens.co.il |
| כתובת | בית ישראל 2, ירושלים |
| טלפונים | 055-2775488 · 055-7799043 |
| אימייל | bk0552775488@gmail.com |
| וואטסאפ | 972552775488 |

### ⚠️ אזהרה קריטית
**ABB Kitchens (חדרה) היא חברה אחרת לגמרי.** בגוגל דרייב של הלקוח יש תיקייה `01 טקסטים לפי עמודים` שמכילה תוכן של ABB — **אסור להשתמש בה.**

## 2. Visual Direction

**טוקנים**

```css
--color-bg: #ffffff;
--color-fg: #1a1a1a;
--color-muted: #767676;
--color-header-bg: #242323;   /* פחם */
--color-surface: #f0f0f0;     /* רקע אודות ופוטר */
--color-border: #e7e7e7;

--color-gold: #ddb143;        /* זהב ממשק — כפתורי פעולה */
--color-logo-gold: #aa8347;   /* זהב הלוגו — נמדד מהקובץ. מספרים, מרקר */

--side-padding: 5vw;
--space-1..7: 8 / 16 / 24 / 32 / 40 / 64 / 88 px
--ease-soft: cubic-bezier(0.32, 0, 0.16, 1);
```

⛔ **שני הזהבים שונים ולא להחליף ביניהם.** `--color-gold` לפעולות · `--color-logo-gold` לזהות המותג.

**טיפוגרפיה:** Polin, 3 משקלים (300/400/700) ב-`public/fonts/`.
⚠️ **הפיגמה משתמשת ב-Ploni — אנחנו תמיד ב-Polin.** לא להעתיק את הפונט משם.

**מבנה דף הבית:** `Hero → About → StyleGrid → Values → TrustedBy → LeadBanner → Testimonials → Footer`

**דפוסים לשימוש חוזר**

| רכיב / מחלקה | מה זה עושה |
|---|---|
| `Reveal.tsx` | עוטף שמוסיף `is-visible` בכניסה למסך. מכבד reduced-motion |
| `Icon.tsx` | ספריית אייקונים אחת, כולם stroke 1.5 על גריד 24 |
| `renderRichText()` | `**מודגש**` |
| `renderHighlight()` | `==מרקר==` בזהב הלוגו |
| `.reveal-mask` | כותרת עולה מתוך מסכה |
| מרקיזה | שתי קבוצות זהות, `min-width: 100%`, translateX. בלי JS |

## 3. References

*(לא תועדו רפרנסים מהמאגר. הפיגמה שימשה כמקור.)*

## 4. Design Decisions

| # | ההחלטה | הנימוק |
|---|---|---|
| 1 | **נתיבים בעברית נשמרו** — תיקיות באנגלית + rewrites ב-`next.config.ts` | שימור SEO מהאתר הישן |
| 2 | **סליידרים נגזרים מ-`slides.length`** | הוספה/הסרה מסתדרת לבד |
| 3 | **לוגואים על קנבס אחיד 360×128** | היישור מובטח ללא תלות ביחס המקורי |
| 4 | **noindex בכל דיפלוי שאינו production** דרך `VERCEL_ENV`, בשתי שכבות | למנוע אינדוקס של תצוגות |
| 5 | **ותק = 30 שנה** | האתר הישן אומר במפורש "למעלה מ-30 שנה" — מאושש |
| 6 | **10.08.26 — CMS על Sanity** עם עריכה ויזואלית click-to-edit, בצורת פאנל אדמין של וורדפרס (סייד-בר + רשימה + מסך עריכה) | דניאל רצה גם וגם; Sanity היחיד מהשניים שעושה עריכה על העמוד. שני דמואים קליקביליים אושרו לפני תחילת המיגרציה |
| 7 | **סדר היקף מוסכם:** טקסט/תמונות → וריאנטים לסקשנים → שליטה עיצובית גלובלית → תיבת לידים → SEO וגלריה | — |

**פוטר:** שתי עמודות במובייל — `RULES.md §11`. הכלל **נולד בפרויקט הזה**.

## 5. Rejected Decisions ⛔

| מה הוצע | למה נדחה | לא להציע שוב |
|---|---|---|
| **וידאו בסקשן האודות** | הפיגמה לא כללה אותו. `VideoPlayer.tsx` **נמחק** | ✅ |
| **Payload כ-CMS** | אין עריכה ויזואלית על העמוד. נשאר כחלופה אם התמחור לכיסא של Sanity יכאיב | חלופה בהמתנה |
| **Tailwind** | CSS מותאם ב-BEM. *(tailwind עדיין יושב ב-devDependencies — שארית)* | ✅ |
| **בניית סקשנים מאפס · multi-site · בלוג** | מחוץ להיקף הנוכחי | ✅ |
| **אייקונים ליד המספרים** בעמוד אודות | המספרים בזהב הלוגו נושאים את עצמם | ✅ |

## 6. Content / SEO

- נתיבים בעברית נשמרו למיגרציה.
- 11 המלצות אמיתיות עם שמות שוחזרו מארכיון האתר הישן ל-`docs/old-site-content.md`. שבע באתר.
- ⚠️ עמודים משפטיים נכתבו על ידי Claude — **חייבים בדיקת עורך דין**, במיוחד הצהרת הנגישות.

## 7. Technical

| | |
|---|---|
| **Stack** | Next.js 16 · App Router · TypeScript · Turbopack · **CSS מותאם ב-BEM, לא Tailwind** |
| **Repo** | `danielr1996v-a11y/ab-kitchens` |
| **ענף** | `client-preview` — ⛔ **לעולם לא לדחוף ל-`main` בלי אישור** |
| **תצוגה חיה ללקוח** | `ab-kitchens-git-client-preview-studio-reznik-kitchen.vercel.app` (URL קבוע, נבנה מחדש בכל push) |
| **פריסה** | Vercel, פרויקט `studio-reznik kitchen` |
| **פיגמה** | fileKey `KG2iA3wDEIlaDQxNRuX8HZ` · אודות `2002:3521` · באנר מובילים `2016:144` · פוטר `2022:1255` |
| **CMS** | Sanity — **חסום:** דניאל צריך לספק `projectId`. ⚠️ `sanity@6` דורש Node ≥22.12 והמכונה על Node 20 → `nvm use 22` |
| **מדיניות Push** | ⛔ **לחכות.** לקמט מקומית, לדווח "מוכן לדחיפה" + hash. הלינק משותף עם אברהם — כל push פונה החוצה |

**חוקי עבודה ספציפיים לפרויקט**

1. **בלי CMS** אלא אם התבקש במפורש *(גובר עכשיו על ידי החלטה 6 — Sanity אושר)*
2. **משימה אחת בכל פעם.** לסיים, להציג, לחכות לאישור
3. **⚠️ יחידות:** אופקי ב-`vw`, **גבהים ב-`vh`**, גובה שורה ב-`em`. *(חורג מ-`RULES.md §0` שאומר הכל ב-vw — נרשם כשאלה פתוחה ב-`LESSONS.md`)*
4. **5vw ריפוד צדדי** דרך `var(--side-padding)`
5. **מחלקות BEM** ב-`app/globals.css`, לא CSS-in-JS
6. **בלי קוד מת.** משהו יוצא משימוש — נמחק מיד
7. **הפיגמה היא סמכות** — אבל הוראה מפורשת של דניאל גוברת עליה
8. **תמיד להריץ `npm run build`** לפני שמדווחים שסיימנו

**כלי העבודה של דניאל:** `DevInspector` (כפתור "בחר אלמנט" בפינה) שולח בקשות תיקון בפורמט קבוע — סקשן, סלקטור, מצב נוכחי, ומה לשנות. חסום ב-production.

## 8. Current Status

**✅ הושלם** — דף הבית · פוטר 2 עמודות במובייל · סבב מובייל (23 מטרות מגע תוקנו) · פינוי הדר צף · שחזור ההמלצות · אישוש הוותק

**🔄 בתהליך** — CMS על Sanity

**⬜ נשאר** — **עמוד אודות** (מבנה מאושר, טרם נבנה)

**🚧 חסום על דניאל / הלקוח**

1. **הטופס בפוטר לא מחובר לשום יעד** (`action="#"`) — **פנייה תיעלם**
2. **אין אף תמונה אמיתית** — הכל סטוק, וזה **סותר את הטענה "אנחנו המפעל"**. נדרשים צילומים של אברהם, הנגרייה ואולם התצוגה
3. `projectId` של Sanity
4. עמודים משפטיים — בדיקת עורך דין
5. **פרופיל Google Business כפול** — מפצל SEO מקומי
6. נתוני SEO למיגרציה: Search Console, Analytics, sitemap מ-Duda
7. לוודא מול פרופיל הגוגל שכל 11 ההמלצות באמת 5 כוכבים

---

## נספח — עמוד אודות (מבנה מאושר, טרם נבנה)

**בלי תמונות** (אין חומר גלם). הטיפוגרפיה נושאת את העמוד.

1. כותרת — "30 שנה של מטבחים"
2. הסיפור — שתי פסקאות, מרקר על "היא המפעל עצמו"
3. מספרים — `30` שנות ניסיון · `2` דורות · `מאות` מטבחים. בזהב הלוגו, **בלי אייקונים**
4. **התהליך — 4 שלבים.** פריסה מאושרת: רשימה מימין, לוח מתחלף משמאל בריחוף
5. למה אנחנו — ארבעת הבידולים, מעמיק ולא משכפל את דף הבית
6. CTA — הפאנל הכהה של LeadBanner בלי תמונה

```
01 פגישה ותכנון        — פגישה, מידות, תוכנית והדמיה, בחירת גימורים
02 הצעת מחיר ואישור    — מחיר סופי, בלי הפתעות, לוח זמנים ברור
03 ייצור במפעל שלנו    — נגרות ושיש אצלנו, לא קבלני משנה  ← הטענה המרכזית
04 התקנה ואחריות       — התקנה, כוונון, ואחריות שממשיכה
```

⚠️ **במובייל אין ריחוף** — הפירוט צריך להיות פתוח תמיד.
